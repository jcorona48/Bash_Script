#!/usr/bin/env bash

exec 2> /dev/null
trap 'echo "Solo se permite un argumento."' ERR

if (($# == 1)); then 

	echo "Bienvenido..."
	else
	[ "$num" -eq "$num"]
	status=$?
	echo "El estado actual es: $status"
fi
